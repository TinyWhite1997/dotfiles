# Personal Copilot instructions

## Git push behavior

- After resolving merge conflicts, always `git push` the branch directly when done — do not ask the user for confirmation first.
- After addressing PR review comments, always `git push` the branch directly when done — do not ask the user for confirmation first.
- This applies even when a PR already exists; just push. Never force-push unless the user explicitly asks.
